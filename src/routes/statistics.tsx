import { jsx } from "@/lib/template";
import { Component } from "@/lib/ui/Component";
import { Flex } from "@/lib/ui/Flex";

export const render = Component({
    render: () => (
        <Flex id="statistics" direction="column" fullWidth fullHeight>

        </Flex>
    )
});
